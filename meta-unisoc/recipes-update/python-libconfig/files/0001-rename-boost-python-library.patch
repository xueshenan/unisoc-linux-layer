diff --git a/setup.py b/setup.py
index cf538d9..b8ca32e 100644
--- a/setup.py
+++ b/setup.py
@@ -4,29 +4,29 @@ from setuptools import setup, Extension
 from os.path import join, basename, splitext
 from glob import glob
 
-include_dirs = ["/usr/local/include", "/opt/local/include"]
-library_dirs = ["/usr/lib", "/usr/lib64", "/usr/local/lib", "/usr/local/lib64", "/opt/local/lib"]
+#include_dirs = ["/usr/local/include", "/opt/local/include"]
+#library_dirs = ["/usr/lib", "/usr/lib64", "/usr/local/lib", "/usr/local/lib64", "/opt/local/lib"]
 libraries = ["config++"]
 
 # Attempt to find libboost_python.so or some variant by searching through the
 # library directories.
 # TODO: is there some API for this?
-for d in library_dirs:
-    libs = glob(join(d, "libboost_python.so"))
-    if not(libs):
-        libs = glob(join(d, "libboost_python*.so"))
-    if libs:
-        libname = basename(libs[0])         # basename
-        libname = splitext(libname)[0]      # truncate postfix
-        libname = libname[3:]               # truncate "lib"
-        libraries.append(libname)
-        break
+#for d in library_dirs:
+#    libs = glob(join(d, "libboost_python.so"))
+#    if not(libs):
+#        libs = glob(join(d, "libboost_python*.so"))
+#    if libs:
+#        libname = basename(libs[0])         # basename
+#        libname = splitext(libname)[0]      # truncate postfix
+#        libname = libname[3:]               # truncate "lib"
+#        libraries.append(libname)
+#        break
 
 # If we were unable to find the shared library go ahead in a default. It might
 # be in an unofficial directory and an environment variable has been set that
 # will point the compiler to it.
 if len(libraries) <= 1:
-    libraries.append('boost_python')
+    libraries.append('boost_python38')
 
 setup(
     name='pylibconfig',
@@ -44,8 +44,8 @@ setup(
         Extension(
             "pylibconfig",
             ["src/pylibconfig.cc"],
-            include_dirs=include_dirs,
-            library_dirs=library_dirs,
+            #include_dirs=include_dirs,
+            #library_dirs=library_dirs,
             libraries=libraries
         )
     ]
diff --git a/src/pylibconfig.cc b/src/pylibconfig.cc
index becaeee..a45f1d2 100644
--- a/src/pylibconfig.cc
+++ b/src/pylibconfig.cc
@@ -116,6 +116,12 @@ public:
         config->lookup ( path ).remove ( name );
     }
 
+    void removeByIndex ( const char * path, unsigned int index )
+    {
+        config->lookup ( path ).remove ( index );
+    }
+
+
     void addBoolean ( const char * path, const char * name )
     {
         config->lookup ( path ).add ( name, libconfig::Setting::TypeBoolean );
@@ -239,5 +245,6 @@ BOOST_PYTHON_MODULE ( pylibconfig )
         .def("setValue", &pyConfig::setValue_int )
         .def("setValue", &pyConfig::setValue_str )
         .def("appendToList", &pyConfig::appendToList )
+        .def("remove", &pyConfig::removeByIndex )
     ;
 }
