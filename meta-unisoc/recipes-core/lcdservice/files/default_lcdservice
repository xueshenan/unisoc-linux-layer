# Default settings for lcdservice.
# Please do not edit this file directly - use `dpkg-reconfigure lcdservice' to
# change the options.

START_DAEMON="false"

# it should be less than suspend timeout commanly, and it's ok
# as we will listen suspend signal to do specific screen action.
SCREEN_TIMEOUT=5

# define device path
SCREEN_OUTPUT_DEV="/sys/devices/platform/display-subsystem/graphics/fb0/blank"
BACK_LIGHT_DEV="/sys/class/backlight/sprd_backlight/brightness"
TOUCH_SCREEN_DEV="/sys/touchscreen/ts_suspend"

# joint the action command line and parse it in main,you could modify them.
SCREEN_ON_ACTION="$SCREEN_OUTPUT_DEV,0;$BACK_LIGHT_DEV,33;$TOUCH_SCREEN_DEV,0"
SCREEN_OFF_ACTION="$SCREEN_OUTPUT_DEV,1;$BACK_LIGHT_DEV,0;$TOUCH_SCREEN_DEV,1"

# startup parameter
LCDSVC_OPTIONS="-n $SCREEN_ON_ACTION -f $SCREEN_OFF_ACTION -t $SCREEN_TIMEOUT"

