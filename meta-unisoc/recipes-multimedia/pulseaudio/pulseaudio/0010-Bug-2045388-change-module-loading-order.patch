diff --git a/src/daemon/system.pa.in b/src/daemon/system.pa.in
index 327f49e..d5d4d75 100755
--- a/src/daemon/system.pa.in
+++ b/src/daemon/system.pa.in
@@ -19,11 +19,6 @@
 # mode.
 changequote(`[', `]')dnl Set up m4 quoting
 
-### Automatically restore the volume of streams and devices
-load-module module-device-restore
-load-module module-stream-restore
-load-module module-card-restore
-
 ### Automatically load driver modules depending on the hardware available
 ifelse(@HAVE_UDEV@, 1, [dnl
 .ifexists module-udev-detect@PA_SOEXT@
@@ -52,6 +47,11 @@ load-module module-native-protocol-unix
 load-module module-device-manager do_routing=1 on_hotplug=1 on_rescue=1
 .endif
 
+### Automatically restore the volume of streams and devices
+load-module module-device-restore
+load-module module-stream-restore
+load-module module-card-restore
+
 ### Automatically restore the default sink/source when changed by the user
 ### during runtime
 ### NOTE: This should be loaded as early as possible so that subsequent modules
